name: Deploy Frontend to VPS

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    env:
      DEPLOY_PATH: /opt/thedudeabides-store/storefront
      ARTIFACT_NAME: deploy.tar.gz

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - run: npm ci

      - run: npm run build

      - name: Create deploy tarball
        run: |
          tar -czf ${{ env.ARTIFACT_NAME }} dist package.json package-lock.json

      - name: Start ssh-agent
        uses: webfactory/ssh-agent@v0.9.0
        with:
          ssh-private-key: ${{ secrets.VPS_SSH_KEY }}

      - name: Add VPS to known hosts
        run: |
          mkdir -p ~/.ssh
          ssh-keyscan -H ${{ secrets.VPS_HOST }} >> ~/.ssh/known_hosts

      - name: Copy artifact to VPS
        run: scp ${{ env.ARTIFACT_NAME }} ${{ secrets.VPS_USER }}@${{ secrets.VPS_HOST }}:~/

      - name: Deploy on VPS
        run: |
          ssh ${{ secrets.VPS_USER }}@${{ secrets.VPS_HOST }} <<'ENDSSH'
            set -euo pipefail
            DEPLOY_PATH=/opt/thedudeabides-store/storefront

            sudo systemctl stop dude-storefront || true
            
            cd $DEPLOY_PATH
            rm -rf dist
            tar -xzf ~/deploy.tar.gz
            rm -f ~/deploy.tar.gz
            
            # Install production dependencies
            npm ci --omit=dev
            
            sudo chown -R $USER:$USER .
            sudo systemctl daemon-reload
            sudo systemctl start dude-storefront
            sleep 3
            sudo systemctl status dude-storefront --no-pager
          ENDSSH

      - name: Health check
        run: |
          sleep 10
          curl -f https://dudeabides.wopr.systems/ || exit 1
